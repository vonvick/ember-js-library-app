<div class="jumbotron text-center">
  <h1>Coming Soon</h1>

  <br/><br/>

  <p>Don't miss our launch date, request an invitation now.</p>

  <div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
    {{#if responseMessage }}
      <div class="alert alert-success">
        <h4>Thank you! Your message was sent.</h4>
        <p>Email: {{ model.email }}</p>
        <p>Reference ID: {{ model.id }}</p>
      </div>
    {{/if}}

      <form class="form-horizontal">
        <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-6 col-md-offset-2">
          {{input
            type="email"
            value=model.email
            class="form-control"
            placeholder="Please type your e-mail address."
            autofocus="autofocus"
          }}
        </div>
        <div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-4 col-md-2">
          <button
            disabled={{ model.isDisabled }}
            class="btn btn-primary btn-md btn-block"
            {{action 'saveInvitation' model}}
          >Request invitation</button>
        </div>
      </form>
    </div>
  </div>

  <br/><br/>
  <br/><br/>
</div>
